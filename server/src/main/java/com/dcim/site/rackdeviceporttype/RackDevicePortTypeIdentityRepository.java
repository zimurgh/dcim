package com.dcim.site.rackdeviceporttype;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RackDevicePortTypeIdentityRepository extends JpaRepository<RackDevicePortTypeIdentity, Long> {
}
