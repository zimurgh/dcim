package com.dcim.connectivity.chargetype;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ChargeTypeIdentityRepository extends JpaRepository<ChargeTypeIdentity, Long> {
}
