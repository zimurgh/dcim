package com.dcim.connectivity.crossconnect;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CrossConnectIdentityRepository extends JpaRepository<CrossConnectIdentity, Long> {
}
